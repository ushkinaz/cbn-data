name: Pull and Process Cataclysm-BN Data

on:
  schedule:
    - cron: '40 */12 * * *'
  workflow_dispatch:
    inputs:
      target_branch:
        description: 'Branch to push data to'
        required: false
        default: 'main'
        type: choice
        options:
          - main
          - dev

jobs:
  pull-and-process:
    name: Pull Data & Convert GFX
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout action branch
        uses: actions/checkout@v4
        with:
          ref: action
          fetch-depth: 1
      
      - name: Checkout data branch
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target_branch || 'main' }}
          fetch-depth: 1
          path: data_workspace

      - uses: actions/setup-node@v3
        with:
          node-version: '24.x'
          cache: 'yarn'

      - run: yarn install --frozen-lockfile --ignore-engines

      - name: Pull game data
        uses: actions/github-script@v8
        env:
          DATA_BRANCH: ${{ inputs.target_branch || 'main' }}
          WORKSPACE_DIR: data_workspace
        with:
          script: |
            const { default: run } = await import('${{ github.workspace }}/pull-data.mjs')
            await run({ github, context })

      - name: Install WebP tools
        run: sudo apt-get update && sudo apt-get install -y webp

      - name: Convert PNGs to WebP
        working-directory: data_workspace
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔄 Starting GFX PNG to WebP conversion"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          
          total_converted=0
          total_failed=0
          
          # Iterate over data/DATE directories
          for build_dir in data/*/; do
            if [ ! -d "$build_dir" ]; then continue; fi
            build_name=$(basename "$build_dir")
            
            # Skip special directories
            if [[ "$build_name" == "latest"* ]]; then continue; fi
            
            # Check if gfx directory exists
            if [ ! -d "${build_dir}gfx" ]; then continue; fi
            
            echo "📦 Processing $build_name build"
            build_converted=0
            build_failed=0
            
            # Iterate over data/DATE/gfx/TILESET directories
            for tileset_dir in "${build_dir}gfx"/*/; do
              if [ ! -d "$tileset_dir" ]; then continue; fi
              tileset_name=$(basename "$tileset_dir")
              
              # Find all PNG files in this tileset
              png_files=$(find "$tileset_dir" -type f -name "*.png" 2>/dev/null)
              png_count=$(echo "$png_files" | grep -c "\.png$" || true)
              
              if [ "$png_count" -gt 0 ]; then
                echo "  🎨 Processing $tileset_name ($png_count PNGs)"
                
                # Convert each PNG to WebP
                for png_file in $png_files; do
                  webp_file="${png_file%.png}.webp"
                  
                  # Convert PNG to WebP with quality 80
                  if cwebp -q 80 "$png_file" -o "$webp_file" >/dev/null 2>&1; then
                    # Delete the original PNG after successful conversion
                    rm "$png_file"
                    build_converted=$((build_converted + 1))
                  else
                    echo "    ⚠️  Failed to convert: $(basename "$png_file")"
                    build_failed=$((build_failed + 1))
                  fi
                done
              fi
            done
            
            if [ "$build_converted" -gt 0 ]; then
              echo "  ✅ Converted $build_converted files from $build_name"
              if [ "$build_failed" -gt 0 ]; then
                echo "  ⚠️  Failed: $build_failed files"
              fi
              total_converted=$((total_converted + build_converted))
              total_failed=$((total_failed + build_failed))
            else
              echo "  ✅ No PNGs to convert in $build_name"
            fi
            echo ""
          done
          
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 Summary: Converted $total_converted files"
          if [ "$total_failed" -gt 0 ]; then
            echo "⚠️  Failed: $total_failed files"
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: Commit and push all changes
        working-directory: data_workspace
        run: |
          # Check if there are any changes
          if [[ -n $(git status --porcelain) ]]; then
            echo ""
            echo "💾 Committing all changes..."
            git config user.name "HHG2CBN Update Bot"
            git config user.email "hhg2cbn@users.noreply.github.com"
            git add -A
            
            # Create commit message
            COMMIT_MSG="Update data and convert GFX to WebP"
            if [ -f builds.json ]; then
              LATEST_BUILD=$(jq -r '.[0].build_number' builds.json)
              if [ "$LATEST_BUILD" != "null" ]; then
                COMMIT_MSG="Update data for $LATEST_BUILD"
              fi
            fi
            
            git commit -m "$COMMIT_MSG"
            git push origin HEAD:${{ inputs.target_branch || 'main' }}
            echo "✅ Successfully pushed all changes"
          else
            echo ""
            echo "ℹ️  No changes to commit"
          fi

